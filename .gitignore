# OS / editor
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Deps (por si más adelante aparece tooling)
node_modules/

# Renderer outputs for manual inspection
tools/render/output-examples/
tools/render/test-output.html

# Skills installed locally (reproducible from skills-lock.json)
.agents/

# Docs internas / dev — no se publican (el repo va al cliente)
CLAUDE.md
RENDER-CONTRACT.md
.impeccable/
.claude/
brief-editor/

# Transcripts de clientes reales — solo se trackean los simulados
transcripts/*
!transcripts/simulado.txt
!transcripts/simulado-transcripcion.txt
